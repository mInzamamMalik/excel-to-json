'use strict';

var _strings = require('./strings');

function toLowerAndNoSpaceStringsArray(arr) {
	arr = arr || [];
	return arr.map(function (i) {
		return (0, _strings.toLowerAndNoSpace)(i);
	});
}

module.exports = {
	toLowerAndNoSpaceStringsArray: toLowerAndNoSpaceStringsArray
};